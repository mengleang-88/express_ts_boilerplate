PORT=3223

# Dockerized MongoDB configuration
MONGO_PORT=27017
MONGO_INITDB_ROOT_USERNAME=root
MONGO_INITDB_ROOT_PASSWORD=password

# App connection string example (adjust credentials/db as needed)
MONGO_URI=mongodb://root:password@localhost:27017/ir_db?authSource=admin
DATABASE_URL=${MONGO_URI}
